[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    gpu: requires a GPU and/or downloaded model weights (deselect with '-m "not gpu"')
    slow: pays a one-off numba JIT (deselect with '-m "not slow"')
